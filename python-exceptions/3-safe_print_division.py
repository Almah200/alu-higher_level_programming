#!/usr/bin/python3
def safe_print_division(a, b):
    try:
        return a / b
    except typeError::
        print ('Not an integer')
    finally:
        print('{} / {} = {}'.format((a, b, safe_print_division(a, b))
        print('Inside result:')

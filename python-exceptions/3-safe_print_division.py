#!/usr/bin/python3
def safe_print_division(a, b):
    try:
        return int(a) / int(b)
    except typeError::
        print ('Not an integer')
    finally:
        print('{}'.format(safe_print_division(int(a) / int(b))
        print('Inside result:')

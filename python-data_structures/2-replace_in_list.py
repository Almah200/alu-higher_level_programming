#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    for i in my_list:
        if [edx] = -[edx]
        return my_list
    if [edx] > range(len(my_list):
            return my_list
    else:


#!/usr/bin/python3
def element_at(my_list, idx):
    for i in my_list:
        if index == - index:
            return None
        elif index > range(len(my_list)):
            return None
        else:
            return my_list[]
